/**
 * Keyframes (keyframes.styl)
 * --------------------------
 * @author Marko Radak <@iammarkoradak>
 * @since  0.1.0
 * @desc   Updates Stylus native vendors variable
 *         used in vendor-prefixing keyframes.
 *         Uses information from 'support.json'.
 */

// all
if prefix-webkit('animation') && prefix-moz('animation')
  vendors = webkit moz official

// webkit only
else if prefix-webkit('animation') && !prefix-moz('animation')
  vendors = webkit official

// moz only
else if !prefix-webkit('animation') && prefix-moz('animation')
  vendors = moz official

// unprefixed
else
  vendors = official
